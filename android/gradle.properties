# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false
android.packagingOptions.pickFirsts=**/libfbjni.so

# Whether the app is configured to use edge-to-edge via the app config or `react-native-edge-to-edge` plugin
# manual: corrected 2026-08-20 — was stale/false from a prior prebuild. RN's own EdgeToEdgePackage
# (expo-modules-core) already calls WindowUtilKt.enableEdgeToEdge() unconditionally on activity
# create regardless of this flag (see node_modules/expo-modules-core/.../EdgeToEdgePackage.kt), and
# targetSdk 36 forces edge-to-edge with no opt-out either way — so this was documentation drift,
# not a functional switch. Flipped to true to match app.json and actual behavior.
expo.edgeToEdgeEnabled=true

# manual: PERF-2 (docs/audit-2026-07.md) — enable R8/ProGuard minification for release
# builds. See android/app/proguard-rules.pro for the keep rules this requires.
android.enableProguardInReleaseBuilds=true

# manual: PERF-2 — enable resource shrinking for release builds, separate flag/commit
# from minification above so either half can be reverted independently if it breaks
# something (shrinkResources can drop a resource still referenced by name/reflection,
# a different failure mode than R8 code minification).
android.enableShrinkResourcesInReleaseBuilds=true
