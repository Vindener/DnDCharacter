import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  content: { flex: 1 },
  statsRow: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    paddingVertical: 12,
  },
  attributes: {
    color: '#fff',
  },
});
