{
  "fixedSectionOk": true,
  "nodesRecovered": 13,
  "edgesRestored": 62,
  "crossBatchEdgesAdded": 0,
  "typesRemapped": 0,
  "complexityRemapped": 0,
  "notes": [
    "Fixed section sanity check: the mechanical 'Fixed' section only listed 2 tested_by edge drops (orphan endpoint / test<->test pairs) plus the tested-by linker adding 2 supplemental edges and tagging 65 production nodes as tested. No node-ID prefix corrections were logged at all for this run, so there is no systemic upstream ID-quality issue to flag (0% of 1065 nodes needed correction).",
    "Scanned all 41 batch files directly: zero nodes with missing 'id', zero unknown node types (only file/function/class/document/config appear), zero unknown complexity values (only simple/moderate/complex appear). Steps 2's 'no id' / 'unknown type' / 'unknown complexity' categories were empty for this assembly run.",
    "48 of the 62 dropped edges were the config: vs file: prefix mismatch described in the dispatch: file-analyzer agents emitted `imports` edges targeting `file:<path>` for i18n locale JSON (src/i18n/locales/{en,uk}/*.json, 34 edges), SRD data JSON (src/data/srd/*.json, 12 edges), and uk domain locale JSON (src/data/locales/uk/{monsters,spells}.json, 2 edges) -- but the actual nodes were legitimately created as `config:<same path>` per the fileCategory mapping. Verified a config: node exists at the identical path for all 48, rewrote each edge's target to the config: id, and restored the edge (all as type imports, existing direction/weight preserved: forward/0.7).",
    "The remaining 14 dropped edges were `calls` edges into functions that were never extracted as nodes by any batch (not an ID-mismatch case). Verified via grep against the actual source files that all 13 distinct target functions are real, exported, non-trivial functions (e.g. src/shared/helpers/combat.ts:abilityMod, src/domain/srd/srdRepository.ts:{getSrdReferences,getConditions,getEquipment}, src/domain/mappers/spell.mapper.ts:{draftToEntity,normalizeStoredSpellbookSpell}, src/shared/const/experience.ts:getLevelByExperience, plus barrel re-exports src/dm/domain/encounter/index.ts:evaluateEncounterDifficulty, src/dm/domain/initiative/index.ts:{rollInitiativeFor,sortByInitiative}, src/shared/services/auth/index.ts:{useAuth,onGoogleButtonPress}, src/domain/schemas/index.ts:parseCharacter). They were simply missed by the significance filter / not covered granularly in whichever batch analyzed that file. Recreated each as a function node with the sensible defaults specified (summary: 'No summary available', tags: ['untagged'], complexity: 'moderate') and restored the 14 calls edges (one function, parseCharacter, is a target of 2 separate call edges from createEmptyCharacter and createCharacterStoreEffects).",
    "Step 3 cross-batch check: cross-referenced all 264 non-empty entries in scan-result.json's importMap (521 total) against assembled-graph.json's imports edges, resolving each import target to either a file: or config: node. Found exactly 48 gaps, and they are the identical 48 edges already recovered in Step 2 (12 SRD json, 2 uk locale json, 34 i18n locale json) -- confirming there is no additional cross-batch edge gap beyond the config/file prefix issue already handled. No speculative edges were added.",
    "assembled-graph.json now contains 1078 nodes (1065 + 13 recovered functions) and 2398 edges (2336 + 62 restored). Verified post-fix: zero dangling edges, zero duplicate node ids, zero duplicate (source,target,type) edge triples."
  ]
}
