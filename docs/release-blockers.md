# Блокери релізу й відкриті питання

Живий документ. Оновлюється, коли пункт закривається або з'являється новий.
Джерело — аудит 2026-08-12 (`docs/audit-2026-07.md`, `docs/release-plan-google-play.md`)
проти реального стану коду й команд.

---

## Блокери релізу

Реальні, не документаційні — код/конфігурація, яку ще треба змінити або
рішення, яке ще не ухвалене.

### Критичний шлях

1. ⏳ **R1-1 — 14-денний відлік у процесі (перевірено 2026-08-12, скрін
   «Робоча версія» власника продукту).** Прогрес відносно попередньої
   перевірки істотний: чекліст «Подання заявки на доступ до робочої версії»
   тепер показує два з трьох пунктів виконаними —
   ~~«Опублікувати версію для закритого тестування»~~ ✅ і
   ~~«Залучити принаймні 12 користувачів до закритого тестування»~~ ✅.
   Це означає: Google схвалив випуск `1.0.0 (4)` (попередній стан
   «Перевіряється» закритий), і 12 реальних тестувальників уже приєднались.
   Третій пункт — **«Провести закрите тестування тривалістю принаймні
   14 днів зі щонайменше 12 користувачами»** — досі відкритий (○), кнопка
   «Подати заявку» неактивна. **Дата старту підтверджена власником
   продукту: 2026-08-12.** Отже 14-денне вікно закінчується **2026-08-26**
   — це найраніша дата, коли кнопка «Подати заявку» може стати активною
   (за умови, що всі 12 тестувальників лишаються opted-in безперервно всі
   14 днів; якщо хтось вийде з тестування, Google може відкинути лічильник
   для цього тестувальника — перевіряти статус ближче до дати).
   Набір людей більше не блокер — залишилось лише перечекати вікно часу,
   код тут нічого не прискорює.

### Технічні

2. ~~**Незакомічені `google-services.json`** (корінь + `android/app/`)~~ — ✅ Виправлено 2026-08-12, — містять
   свіжий SHA-1-фінгерпринт після виправлення R1-2 (2026-08-12), але зміни ще
   не в git (`git status` показує обидва файли modified, unstaged). Наступний
   білд EAS уже підхопить новий OAuth-клієнт локально лише якщо закомітити.
3. ~~**`READ_EXTERNAL_STORAGE`/`WRITE_EXTERNAL_STORAGE`** досі в
   `AndroidManifest.xml` (PLY-3)~~ — ✅ Виправлено 2026-08-12, обидва дозволи
   прибрано з `AndroidManifest.xml` (`app.json` не декларує
   `android.permissions`, тож дублювати правку нема куди).
4. ~~**`console.*` — регрес: 27 → 39** (SEC-9)~~ — ✅ Перевірено 2026-08-12:
   усі 39 викликів гейтяться `if (__DEV__)`/`if (isDev)` або раннім `return`
   під `isStartupTraceEnabled()` (`startupTrace.ts`), тож у релізному білді
   без явного opt-in-флага жоден не виконується. Реального витоку даних
   немає; кількість (27→39) лишається на майбутнє як гігієна коду, не блокер.
5. **`npm audit` — регрес: 22 → 35 → 38** (21 moderate, 17 high, перевірено
   2026-08-13). Здебільшого той самий build/dev-тулчейн-кластер
   (`@expo/config-plugins → xcode`, `metro → image-size`). **Новий нюанс
   2026-08-13:** `nanoid <3.3.17` тепер підсвічується і як залежність
   `@react-navigation/native` (`routers` генерує route-key через `nanoid()`) —
   це вже виконується в самому бандлі застосунку, не лише в білд-тулчейні.
   Вразливість (`GHSA-28wg-ghj8-5hjv`/`GHSA-2v37-7h3g-55p8`) — нескінченний
   цикл при негативному/нульовому `size`, який ніхто в кодовій базі (і сам
   react-navigation) не передає, тож практичного шляху експлойту немає, але
   класифікація «все build-тулчейн» для цього одного пакета вже не точна.
   `fixAvailable: true` без мажора — можна закрити через `overrides`
   (аналогічно SEC-7), але це окрема задача з повним `validate` + локальний
   Android-білд, не зроблено в межах цього аудиту.
6. ~~**R4-2 — живий тест OTA не проведено.**~~ — ✅ Зроблено 2026-08-12,
   підтверджено власником продукту: `eas update` запущено, оновлення
   підхопилось на встановленому білді.
7. **PERF-2 — proguard/shrinkResources досі вимкнені.** Без змін від
   попереднього аудиту. Увімкнення вимагає повного smoke-тесту релізного
   білда (R8 ламає рефлексію в RN Firebase/Reanimated) — не робити без часу
   на цей тест. **Навіщо це взагалі потрібно** (власник продукту питав):
   зменшення розміру APK/AAB (менше завантаження й місця на пристрої),
   базова обфускація коду (складніше реверс-інжинірити), прибирання
   невикористаних ресурсів. Це не блокер релізу 1.0.0 — Play не вимагає
   proguard/shrink, це суто оптимізація. Рекомендація: не робити зараз,
   під час активного закритого тесту — ризик (R8 ламає рефлексію в RN
   Firebase/Reanimated) вищий за вигоду в цьому вікні; повернутися після
   1.0, коли є час на повний smoke-тест.
8. ~~**PLY-5 — донат-текст.**~~ — ✅ Закрито 2026-08-12. Крипто-гаманець і
   банківські посилання (PrivatBank/monobank/USDT) прибрано з `Support.tsx`
   — залишився лише лінк на Telegram-канал (`donationUrl =
   'https://t.me/mythgatednd/12'`), Payments/Financial products ризик
   знято. Текст «збираємо гроші для... публікації в Apple» — **не помилка**,
   підтверджено власником продукту: це свідомий збір на майбутній
   iOS/iPhone-реліз. `uk/support.json:9` і `en/support.json:9` тепер
   синхронні («raising funds to grow our resources and release the app on
   iPhone»).

9. **PLY-1 — `deleteMyAccount` повертає `NOT_FOUND` на реальному пристрої
   (виявлено 2026-08-12, скрін від власника продукту).** Клієнтський код і
   Cloud Function (`functions/src/deleteMyAccount.ts`, регіон `us-central1`,
   проєкт `mythgatednd` — збігається з `google-services.json`) виглядають
   коректно; `functions/lib/*.js` вже збілджені локально. `NOT_FOUND` — це
   код помилки Firebase Functions callable-клієнта саме для «функція не
   знайдена на бекенді», той самий симптом, що вже задокументований у коді
   для `createCampaignInvite` (`DMCampaignDetail.tsx:176-181`). Найвірогідніша
   причина — **`deleteMyAccount`/`createCampaignInvite`/`redeemCampaignInvite`
   ще не задеплойені** на проєкт `mythgatednd`, або не виконано one-time
   Console-передумови з `functions/README.md` (Blaze-план замість Spark,
   увімкнені Cloud Functions/Cloud Build/Artifact Registry/Eventarc API) —
   без Blaze-плану 2nd-gen функції взагалі не деплояться. Це критично для
   Google Play: видалення акаунта — обов'язкова вимога (R1), і зараз вона
   не працює в проді. **Дію не можу виконати сам** — деплой потребує
   `firebase login` під акаунтом-власником проєкту й (можливо) підняття
   білінгу в Firebase Console, це поза можливостями цього середовища.
   Точкова правка зроблена: `DeleteAccountModal.tsx` більше не показує
   користувачу сирий код помилки (`NOT_FOUND`) — тепер завжди generic текст,
   за винятком `reauth-failed`.

   **Підтверджено спробою деплою 2026-08-12:** `firebase deploy` дійшов до
   кроку ввімкнення `artifactregistry.googleapis.com` і впав з `Error: Your
   project mythgatednd must be on the Blaze (pay-as-you-go) plan` — причина
   підтверджена, це не гіпотеза.

   **Рішення власника продукту (2026-08-12):** перейти на Blaze +
   бюджетний алерт, а не переписувати каскад на клієнт без Cloud Functions.
   Обґрунтування: `onCall`-функції викликаються вручну (видалення акаунта,
   інвайт у кампанію) з масштабом закритого тесту ~12 людей — це на порядки
   нижче безкоштовної квоти 2nd-gen Cloud Functions (2 млн викликів/міс),
   очікувана вартість $0/міс. Альтернатива (клієнтський каскад через зміну
   `firestore.rules` + відмова від атомарної admin-транзакції) створює
   реальний ризик для security-межі проєкту за 3 тижні до релізу — обрано
   не робити.

   **Зроблено власником 2026-08-12:** Blaze підключено, бюджетний алерт $1
   налаштовано (Billing → Budgets & alerts). `firebase deploy` після цього
   пройшов далі — `firestore.rules` задеплоїлись успішно (`released rules
   firestore.rules to cloud.firestore`), API (`cloudbuild`, `artifactregistry`,
   `run`, `eventarc`, `pubsub`, `firebaseextensions`) увімкнено автоматично.

   **Новий блокер, знайдений на цьому кроці (той-таки день):** усі три
   функції впали на Cloud Build з `lib/index.js does not exist`. Причина —
   баг у `firebase.json`: `functions[0].ignore` містив `"lib"`. Локальний
   `predeploy`-скрипт (`npm run build`) справді компілює `functions/lib/`,
   але потім пакування джерела для завантаження в Cloud Build застосовує
   той-таки `ignore`-список і викидає щойно зібраний `lib/` з архіву — на
   боці Cloud Build код приїжджає без entry point (`main: "lib/index.js"`
   у `functions/package.json`). **Виправлено**: `"lib"` прибрано з
   `firebase.json` (рядок 9), лишились лише `node_modules`/`.git`/тести.

   Побічні варнінги з того ж запуску деплою (не блокери, не для зараз):
   Node.js 20 runtime deprecation (decommission 2026-10-30 — оновити
   `functions/package.json` `engines.node` заздалегідь, але не в цьому
   релізному вікні) і застаріла версія `firebase-functions` — обидва
   ігнорувати до 1.0, потім окрема задача.

   **Ще один блокер після успішного деплою (той-таки день):** усі три
   функції відповідали `UNAUTHENTICATED` на легітимні виклики з застосунку.
   Причина — Cloud Run-сервіси (2nd gen) деплоїлись з `Require
   authentication` замість `Allow public access` на вкладці Security, тож
   запит відхилявся на рівні IAM ще до того, як код функції встиг
   перевірити `request.auth?.uid`. **Виправлено власником вручну** в Cloud
   Run Console (Services → `deletemyaccount`/`createcampaigninvite`/
   `redeemcampaigninvite` → Security → Allow public access) для всіх трьох
   сервісів. Це безпечно: усі три функції самі перевіряють
   `request.auth?.uid` у коді (`deleteMyAccount.ts:143`,
   `createCampaignInvite.ts:38`, `redeemCampaignInvite.ts:22`) — "публічний"
   тут лише транспортний рівень Cloud Run, авторизація лишається в коді.

   **UI розблоковано в коді (2026-08-12):** кнопки `inviteButton`
   (`DMCampaignDetail.tsx`) і `redeemInviteButton` (`DMCampaigns.tsx`) мали
   хардкоджений `disabled` + приглушений стиль + hint "тимчасово недоступно"
   від коміту `d7cfe86` (свідоме тимчасове вимкнення до деплою). Прибрано
   `disabled`/`opacity`/hint-текст в обох файлах; застарілі ключі
   `inviteCodeDisabledHint`/`redeemInviteDisabledHint` видалено з
   `en/dm.json` і `uk/dm.json`. `typecheck`/`lint`/`lint:ui`/`test:unit`
   зелені без регресу.

   **Видалення акаунта — ✅ підтверджено робочим на реальному пристрої
   (власник продукту, 2026-08-13).** Це закриває основну частину блокера:
   `deleteMyAccount` викликається з застосунку й виконується успішно після
   фіксів Blaze/IAM/`firebase.json` вище. PLY-1 як «видалення акаунта не
   працює» — закрито.

   **Створення інвайту й приєднання по коду — ✅ підтверджено власником
   продукту 2026-08-13.** `createCampaignInvite`/`redeemCampaignInvite` через
   ручне введення коду (`DMCampaigns.tsx:193`) працюють на пристрої.

   **Приєднання по посиланню (`https://mythgatednd.pp.ua/join/:code`) — ❌ не
   працює, потрібне допрацювання веб-версії (підтверджено власником продукту
   2026-08-13).** Корінна причина ймовірно технічна, не в Cloud Function:
   - `AndroidManifest.xml:37-42` декларує App Link `autoVerify="true"` лише
     для хосту `mythgatednd.pp.ua` (без `www.`), `pathPrefix="/join"`. Android
     верифікує App Link лише якщо на цьому ж хості за адресою
     `https://mythgatednd.pp.ua/.well-known/assetlinks.json` лежить файл із
     правильним `package_name` (`com.vind.MythgateDND`) і SHA-256 сертифіката
     підписання (той самий, що й для Google Sign-In, `docs/release-plan-google-play.md`
     §R1-2). Якщо цього файлу немає на сервері — верифікація мовчки не
     проходить, і посилання відкривається в браузері, а не в застосунку.
   - `AppNavigator.tsx` (`linking.prefixes`) очікує **обидва** варіанти —
     `https://mythgatednd.pp.ua` і `https://www.mythgatednd.pp.ua` — а
     нативний intent-filter задекларований лише на один хост без `www.`.
     Навіть після додавання `assetlinks.json` посилання на `www.`-варіант не
     верифікується Android App Links (точний збіг хосту, не піддомену) — це
     окрема нестиковка, яку варто виправити тим самим проходом.
   - Крім самої верифікації, на `https://mythgatednd.pp.ua/join/:code`
     потрібна **фолбек-сторінка** (як уже є для Privacy Policy й видалення
     акаунта на тому самому домені) — для користувачів без застосунку
     встановленого або якщо верифікація не спрацювала на конкретному
     пристрої/ОС.

   **Дію не можу виконати сам** — потрібен доступ до хостингу
   `mythgatednd.pp.ua` (той самий, де вже є `/privacy`), щоб додати
   `.well-known/assetlinks.json` і сторінку `/join/:code`. Це вебова робота,
   не зміна коду застосунку (окрім, можливо, додавання другого `<data
   android:host="www.mythgatednd.pp.ua">` intent-filter в `AndroidManifest.xml`
   для узгодження з `AppNavigator.tsx`).

### Продуктове рішення

10. ~~**REL-6 — білий спалах на старті.**~~ — ✅ Виправлено 2026-08-12: причина
   була не в самому сплеші (він і так брендовий червоний), а у відсутньому
   `android:windowBackground` в `AppTheme` — на мить показувався дефолтний
   білий фон AppCompat між приховуванням сплеша й першим кадром RN. Додано
   `android:windowBackground = @color/splashscreen_background`
   (`android/app/src/main/res/values/styles.xml`). **Не вирішено:** сам
   `userInterfaceStyle: "light"` (примусовий світлий режим) — це лишається
   окремим продуктовим рішенням, не пов'язаним з баґом, що злякав
   тестувальника.
11. ~~**SEC-10 — створення персонажа «Локально + Хмара» падало з
   `permission-denied` (знайдено власником продукту на пристрої).**~~ —
   ✅ Виправлено 2026-08-13. Корінна причина: `characterSheets`-правило
   `allow get, list: if isEditor();` вимагало `resource.data.ownerUid`, а для
   ЩЕ НЕ ІСНУЮЧОГО документа `resource == null` — тож навіть службовий
   `ref.get()` (перевірка "документ новий чи існуючий?" в
   `upsertCharacterSheetFromLocal`) падав з `permission-denied` ще до спроби
   створення. Персонаж ніколи не потрапляв у Firestore; помилки `Listen` в
   `adb logcat` були наслідком, не причиною. Перед цим перевірено й
   відкинуто кілька хибних версій: App Check (не підключений), зсув
   годинника (спростовано — паралельний запис у `users/{uid}` тим самим
   `request.auth` проходив), стара версія правил (деплой підтверджено
   актуальним). Фікс: `firestore.rules` →
   `allow get, list: if resource == null || isEditor();` — дозволяє
   безпечно перевірити "документа ще нема" (там немає даних, нічого не
   витікає), захист існуючих документів не змінився. Перевірено на
   Firestore-емуляторі (`firebase emulators:exec` + `rules-unit-testing`,
   6 сценаріїв, включно з "чужий не власник все ще НЕ читає існуючий
   документ") і підтверджено на реальному пристрої власником продукту.
   Супутньо закрито залишок COL-1 (гонка в транзакційному фолбеку синку,
   окрема, менш критична дірка того самого коду) — деталі SEC-10 в
   `docs/audit-2026-07.md`.

---

## Питання до власника продукту — закриті 2026-08-13

Усі три відкриті питання отримали відповідь власника продукту.

1. ✅ **Публікувати `docs/legal-terms-of-service.md` без юридичної
   перевірки — ТАК.** Рішення власника продукту 2026-08-13. Файл лишається з
   попередженням «не юридична консультація» (це технічно правда й після
   публікації), але публікація на сайті більше не заблокована відсутністю
   юрперевірки — чернетка й рішення про публікацію свідомо розділені, і
   рішення ухвалене. Дія, що лишається: викласти текст на постійному домені
   (той-таки, що й Privacy Policy — `https://www.mythgatednd.pp.ua`) і додати
   лінк у Settings поруч із Privacy Policy — ручний крок, не код.
2. ✅ **Play Console → «Вікові обмеження» / «Цільова аудиторія і контент» —
   13+.** Продиктовано власником продукту 2026-08-13. Узгоджується з чернеткою
   Content Rating у `docs/store-listing.md` §5 (fantasy violence без графіки,
   без сексуального контенту, лексики, азартних ігор) — 13+ виглядає
   послідовно з цими відповідями. Значення зафіксовано в
   `docs/release-plan-google-play.md` R4-6 і `docs/store-listing.md` §5.
3. ✅ **`google-services.json` — уже закомічено, готово.** Підтверджено
   власником продукту 2026-08-13. `git status` на момент цього запису чистий,
   `git log` показує коміт `050594e` («update google-services.json with new
   client IDs for Android app») в історії гілки — файл із оновленими
   client ID вже в репозиторії, наступний білд (EAS чи локальний) підхопить
   його автоматично.
