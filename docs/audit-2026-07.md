# Детальний аудит перед релізом — Mythgate 5e Companion

Ревізія: **v2, 2026-07-26** · Куди покласти: `docs/audit-2026-07.md`
Версії: `app.json` → `0.91.1`, `build.gradle` → `versionName 0.91.1` / `versionCode 1` (підіймається до `1.0.0`)
Ціль: публікація в Google Play. Реалістичну дату дивись у `docs/release-plan-google-play.md` §0.

Джерело: статичний аналіз + реально запущені `npm ci`, `tsc --noEmit`, `eslint`, `vitest`, `npm audit` + перевірка версійного каталогу Android у `node_modules`.

**Зміни в v2:** додано розділ 3 (спільне редагування, знахідки COL-1…COL-10), уточнено PLY-6 (versionName у bare-проєкті), PLY-7 (targetSdk **підтверджено = 36**, edge-to-edge стає обов'язковим), PLY-9 (Play Console не створений — тепер головний фактор дати), PLY-1 переписано з урахуванням спільних аркушів.

**Оновлення статусів 2026-08-12** (документ лишається знімком липня 2026 — дату
в шапці не змінюємо, статуси нижче звірені з реальним кодом і командами
сьогодні): закрито або значно просунуто **COL-1/2/3, SEC-6, REL-1, PERF-3,
PERF-4, PLY-3**; частково закрито **PERF-5**; **SEC-9** — кількість
`console.*` зросла (27→39), але перевірено, що всі виклики гейтяться
`__DEV__`/opt-in-флагом, тож витоку в релізі немає. Позначки `✅ Виправлено 2026-08-12` /
`⚠️ Частково` / `📉 Регрес` розставлені інлайн у відповідних розділах нижче.
Повний перелік з поясненнями — `docs/release-blockers.md`.

**Оновлення статусів 2026-08-13** (повторний прохід — `tsc`/`lint`/`test:unit`
запущені реально, не з попереднього запису): **SEC-10** (нове, критично) —
блокер створення нового персонажа в режимі «Локально + Хмара» знайдено й
виправлено того ж дня, деталі нижче. **PLY-1** — видалення акаунта підтверджено
робочим на реальному пристрої власником продукту, закрито. Питання до
власника продукту з `docs/release-blockers.md` (публікація ToS, вікова
категорія 13+, `google-services.json`) — усі три закриті рішенням від
2026-08-13. Дрібні дрифти без функціонального ризику: тести 328→331 (+3,
приріст, не регрес), `console.*` 39→40 (+1, увесь приріст — гейтований
`isDev` код у Campaign Management, окремому виключенні з release hardening),
**`any` у не-тестовому коді виявився хибним підрахунком — фактично 0, не 1**
(попередній збіг був підрядком «any» усередині коментарів, не типом), `npm
audit` 35→38 (деталі — новий SEC-11).

**Оновлення статусів 2026-08-13 (вечір)** — знахідки від тестувальника
(«не бачу скелетон, ніби зависає» на Character/Bestiary/Spellbook; краш при
швидких тапах «Назад»/«Далі» у створенні персонажа) і живий перегляд Firebase
Crashlytics власником продукту:
- **PERF-1 переоцінено, лишається відкритим, але без «холодного старту»**:
  ліниві геттери й build-time Zod (план з розділу нижче) виявились уже
  реалізованими в коді (`srdRepository.ts`, `localization.ts`, коментарі
  `PERF-1`) — вартість парсингу вже не блокує перший кадр застосунку. Але
  залишкова вартість (найбільше — `uk/monsters.json`+`uk/spells.json` і
  побудова 3 `Map` у `localization.ts`) виконувалась синхронно всередині
  `useMemo`, що якраз замінює скелетон на контент (`Bestiary.tsx` typeOptions/
  sizeOptions, `Spellbook.tsx` classOptions/schoolOptions,
  `useCharacterActions.tsx` sourceFeatureRows) — це і є причина «зависання без
  скелетона» при першому відкритті екрана за сесію. Пом'якшено 2026-08-13:
  прогрів кешу (`warmSrdLocalizationCache`, `warmCharacterSrdCache`) додано в
  `useEffect`/`InteractionManager.runAfterInteractions` на вході в ці три
  екрани, щоб вартість падала на фазу скелетона, а не на рендер, що його
  знімає. **Не перевірено на реальному Android-девайсі** — лише статичний
  аналіз + типи/лінт/тести.
- **PERF-6 фактично закрито, документ не був оновлений**: `Bestiary.tsx` і
  `Spellbook.tsx` вже мають `initialNumToRender`/`maxToRenderPerBatch`/
  `windowSize`/`removeClippedSubviews` — перевірено 2026-08-13, позначено ✅
  нижче.
- **Новий REL-8 · Високий · Crashlytics не завантажував нативні символи
  застосунку.** Три відкриті native-краші в проді (`1.0.0 (7)`, SIGABRT
  `libc.so` regressed 7 events/2 users, 2× SIGSEGV `libminikin.so`/`libc.so`)
  показані з `(Missing BuildId ...)` для `libappmodules.so`/`libreactnative.so`
  — власні символи застосунку не резолвились, бо `android/app/build.gradle`
  не мав `firebaseCrashlytics { nativeSymbolUploadEnabled true }` у
  `buildTypes.release`. Виправлено 2026-08-13 (`android/app/build.gradle`).
  **Наслідок:** точна причина крашу з тестувальника (швидкі тапи «Назад»/
  «Далі») лишається невідомою — символи в поточних трьох issue не
  відновлюються ретроактивно; потрібен новий білд + новий крash того ж типу,
  щоб побачити резолвлений стек. Спекулятивний JS-guard на кнопках
  «Назад»/«Далі» в `CreateCharacter.tsx` було спробувано й **відкочено** того
  ж дня — ламав легітимний швидкий подвійний тап «Далі» (підтверджено тестом
  `CreateCharacter.test.tsx`), а підтвердженого зв'язку з JS-кодом немає:
  краші нативні (SIGABRT/SIGSEGV), не React-виняток.

**Оновлення статусів 2026-08-14** — розібрано два нові Crashlytics native-краші
від тестувальника (обидва SIGSEGV, версія `1.0.2 (8)`), закрито **PERF-2**, і
кілька дрібних знахідок від того ж тестувальника:
- **Новий REL-9 · Критично · Два SIGSEGV-краші від вкладеного
  `KeyboardAwareScrollView` — ✅ Виправлено.** Деталі нижче в §5.
- **PERF-2 — ✅ Виправлено.** Деталі нижче в §4.
- **Дрібні знахідки тестувальника, виправлено того ж дня:** дублювання
  підсвіченого чіпа-«скидання» фільтра класу/школи в Spellbook (чіп показував
  назву обраного значення замість статичного напису, тому виглядало як два
  однакові підсвічені чіпи) — `src/screens/Spellbook/Spellbook.tsx`; клас
  «Ranger» у даних заклять був перекладений як «Слідопит», а не «Рейнджер»,
  як усюди в решті застосунку — `src/data/locales/uk/spells.json` (34
  входження).
- **Новий продуктовий виняток (рішення власника продукту 2026-08-14, не
  порушує release freeze):** автозаповнення стартових заклять за класом у
  створенні персонажа (текстове поле, вільно редагується) + окрема модалка
  вибору заклять 1-го рівня — `src/domain/srd/startingSpells.ts` (новий),
  `CreateCharacter.tsx`, `createCharacterWizard.ts`. Задокументовано як
  «Виняток 2» у `CLAUDE.md`.
- Базова лінія: тести 331→342 (+11, весь приріст — нові тести на щойно
  додану логіку вище, не регрес), `console.*`/`any`/`npm audit` без змін.

---

## 0. Що вже в добрій формі

Фіксую, щоб не «оптимізували» те, що працює:

- **Типізація здорова**: `tsc --noEmit` — 0 помилок, у не-тестовому коді 0 `any` (перевірено 2026-08-13; попередній «1 випадок» був хибним підрахунком підрядка «any» в коментарі, не типом).
- **Тести є і зелені**: 50 файлів, 182 тести, ~28 с (станом на 2026-08-13: 61 файл, 331 тест, ~2.4 с; станом на 2026-08-14: 62 файли, 342 тести). Покриті схеми, мапери, міграції, репозиторії, фільтри, dice roller, DM-логіка, конфлікт-політика, UI-варіанти, скелетони.
- **Архітектура шарована**: `screens` → `stores`/`services` → `repositories` → Firestore/AsyncStorage, явний DTO-мапінг, версіонування схем (`src/domain/migrations`).
- **Дизайн-система реальна**: токени `sp/fs/rd`, `typography()`, примітиви `@/shared/ui`, два лінтери-охоронці.
- **Синхронізація продумана глибше, ніж типовий pet-проєкт**: live-підписки, журнал змін із ролями, секційний merge, явні стани `pending-upload`/`pending-download`/`conflict`, ручне розв'язання конфліктів. Це вже майже правильний каркас — саме тому фікси нижче точкові, а не «переписати синк».
- **targetSdk уже 36** (`react-native/gradle/libs.versions.toml` через `useExpoVersionCatalog`), тобто вимогу Play «target API 36 до 31.08.2026» виконано без роботи.
- **SRD 5.1 (CC-BY-4.0) атрибуція** окремим екраном (`src/screens/LegalLicenses`) — найбільший юридичний ризик D&D-застосунку знятий.
- **Скелетони й стани завантаження** реалізовані, спринти 1–6 закриті.
- **Дебаунс пошуку 250 мс** у Spellbook і Bestiary.

---

## 1. Блокери публікації (PLY)

### PLY-9 · Критично · Play Console не створений — це і є критичний шлях

Перевірено у відкритих джерелах (липень 2026):

- Персональний акаунт розробника, створений після 13.11.2023, зобов'язаний провести **закритий тест: ≥12 тестувальників, opted-in 14 днів безперервно**, і лише потім подавати заявку на production access.
- Акаунти-організації (зареєстрована юридична особа) **звільнені** від цієї вимоги, але для них потрібен **D-U-N-S номер**, отримання якого може займати 30+ днів.
- Верифікація особи для персонального акаунта: від кількох годин до 2 робочих днів.
- Заявка на production access: огляд «зазвичай до 7 днів». Далі ще огляд самого релізу — до 7 днів.
- Реалістичний end-to-end для нового персонального акаунта, за оцінками практиків: **3–5+ тижнів**.

**Наслідок:** публікація в production у середині серпня з нуля недосяжна для персонального акаунта. Детальний перерахунок дати — `docs/release-plan-google-play.md` §0. Правила Google змінюються — **перевір у самій Play Console**, а не за цим документом.

### PLY-1 · Критично · Немає видалення акаунта (складніше через спільні аркуші) — ✅ Виправлено, підтверджено на пристрої 2026-08-13

Первинна знахідка (grep по `deleteUser`/`deleteAccount`/`currentUser.delete` — нічого) уже нерелевантна: код (owner/editor каскад, передача ownership, Cloud Function `deleteMyAccount`, правило `users`) реалізовано ще 26 липня (`docs/release-plan-google-play.md` R1-4). Далі йшов ланцюжок деплой-блокерів (Blaze-план, `firebase.json` `ignore: ["lib"]`, Cloud Run IAM `Require authentication`) — усі задокументовані й закриті 2026-08-12 в `docs/release-blockers.md` п.9.

**Підтверджено власником продукту 2026-08-13:** видалення акаунта викликається з застосунку на реальному пристрої й виконується успішно. PLY-1 закрито повністю.

Той самий деплой/IAM-шлях обслуговує ще дві callable-функції з Campaign Management (окремий свідомий виняток із release hardening, не частина PLY-1): **створення інвайту й приєднання по коду** (`createCampaignInvite`/`redeemCampaignInvite`, ручне введення) — ✅ підтверджено робочим 2026-08-13. **Приєднання по https-посиланню** (`https://mythgatednd.pp.ua/join/:code`) — ❌ не працює: `AndroidManifest.xml:37-42` декларує App Link `autoVerify` лише на хост `mythgatednd.pp.ua` (без `www.`), і на сервері ймовірно відсутній `.well-known/assetlinks.json` — обидва потрібні Android, щоб верифікувати посилання й відкрити застосунок, а не браузер. Плюс `AppNavigator.tsx` очікує ще й `www.`-варіант хосту, якого нема в маніфесті. Деталі й дія — `docs/release-blockers.md` п.9.

Файли: `src/screens/Settings/Settings.tsx`, `src/services/accountDeletion.ts`, `functions/src/deleteMyAccount.ts`, `firestore.rules`.

### PLY-2 · Критично · Немає Privacy Policy — ✅ Виправлено 2026-08-12

Жодного згадування в коді, i18n чи `app.json`. Обов'язкове поле в Play Console. Через спільне редагування текст має додатково розкривати, що **вміст аркуша стає видимим запрошеним співредакторам** і що DM бачить журнал змін гравця — це передача даних між користувачами, і її треба задекларувати в Data Safety.

**Перевірено 2026-08-12 (живий сайт, не з чужих слів):** `https://www.mythgatednd.pp.ua/privacy` — повний текст (розкриття видимості аркуша співредакторам і DM присутнє), обидві мови через кнопку «EN», секція видалення акаунта на тій самій сторінці (`id="account-deletion"`). Лінк у застосунку оновлено на цей URL (`Settings.tsx:22`). Поле в самій Play Console — не перевірено (дія в консолі).

### PLY-3 · Критично · Зайві та чутливі permissions — ✅ Виправлено 2026-08-12

`android/app/src/main/AndroidManifest.xml`: `RECORD_AUDIO`, `SYSTEM_ALERT_WINDOW`, `READ_EXTERNAL_STORAGE`, `WRITE_EXTERNAL_STORAGE`, `requestLegacyExternalStorage="true"`.

Мікрофон приходив із `expo-image-picker` (запис відео), storage — з `react-native-fs` / `expo-media-library`, які **в коді не використовуються** (0 імпортів). Обґрунтувати мікрофон і overlay у D&D-компаньйоні нічим — довгий review, часто відмова.

**Перевірено 2026-08-12:** `RECORD_AUDIO`, `SYSTEM_ALERT_WINDOW` і `requestLegacyExternalStorage="true"` вже були прибрані з `AndroidManifest.xml`. `READ_EXTERNAL_STORAGE`/`WRITE_EXTERNAL_STORAGE` теж прибрано цим проходом — пакети, що їх вимагали (`react-native-fs`, `expo-media-library`), відсутні в `package.json` (див. PERF-3), і `app.json` не декларує `android.permissions`, тож правки лише в `AndroidManifest.xml` достатньо. Усі чотири пункти закриті.

### PLY-4 · Критично · Реліз підписується debug-keystore

`android/app/build.gradle`: `release { signingConfig signingConfigs.debug }`. Play відхиляє debug-підписані AAB. EAS зазвичай підставляє свої credentials, але **перевіряти треба на реальному артефакті**. Окремо: після налаштування Play App Signing з'явиться новий SHA-1, і його треба додати у Firebase + OAuth client, інакше Google Sign-In зламається саме в проді.

### PLY-5 · Високий · Донати й крипта на екрані підтримки

`src/screens/Support/Support.tsx:7-12` — PrivatBank, monobank jar, адреса USDT TRC20. Дві політики Play одночасно: Payments (обхід Play Billing) і Financial products (крипта). Мінімум — прибрати крипто-гаманець; по банківських посиланнях ухвалити свідоме рішення.

### PLY-6 · Високий · Версіонування: `versionName` живе в gradle, не в `app.json`

Уточнено в v2. У bare-проєкті (а він саме bare, бо `android/` закомічений і prebuild не запускається) `versionName` береться з `android/app/build.gradle`. Тобто підйом до 1.0.0 треба зробити **в обох місцях**:

- `app.json` → `"version": "1.0.0"`;
- `android/app/build.gradle` → `versionName "1.0.0"`.

Чому це не косметика: якщо `runtimeVersion` буде з політикою `appVersion`, runtime OTA рахується з `app.json`, а стор показує gradle-значення. Розбіжність = апдейти летять не в ту збірку. `versionCode` не чіпати руками — ним керує EAS (`appVersionSource: "remote"`); після першого білда зафіксувати фактичне значення.

### PLY-7 · Високий · Edge-to-edge тепер обов'язковий (targetSdk підтверджено = 36)

У v1 це був пункт «перевірити». Перевірено: `react-native/gradle/libs.versions.toml` → `targetSdk = "36"`, `compileSdk = "36"`, `minSdk = "24"`; `settings.gradle` викликає `expoAutolinking.useExpoVersionCatalog()`, `app/build.gradle` бере `rootProject.ext.targetSdkVersion`.

Добра новина: вимога Play «API 36 до 31.08.2026» виконана. Погана: при targetSdk 36 системний opt-out від edge-to-edge не діє, а в `android/gradle.properties` стоїть `expo.edgeToEdgeEnabled=false`. Отже на Android 15+ система малює edge-to-edge незалежно від флага, а `react-native-edge-to-edge` не підключений.

Часткова компенсація вже є: `react-native-safe-area-context` + `useSafeAreaInsets` у `AppNavigator`. Але треба пройтися по модалках, sticky-футерах (`CreateCharacter`), `ScrollView` без bottom padding і статус-барах на реальному Android 15/16.

Окремо перевірити відповідність вимозі **16 KB page size** (RN 0.81/Expo 54 її підтримують; Play попередить у консолі).

### PLY-8 · Середній · Trademark у store listing

У метаданих Play не використовувати «Dungeons & Dragons» і логотипи Wizards. «Mythgate 5e Companion» + атрибуція SRD 5.1 за CC-BY-4.0 (не OGL 1.0a — Wizards випустили SRD 5.1 під Creative Commons Attribution 4.0 як окрему, простішу ліцензію; текст обов'язкової атрибуції — `dnd.wizards.com/resources/systems-reference-document`, локальна копія `.agents/docs/SRD_CC_v5.1.pdf`) — безпечна рамка. Ліцензія прямо дозволяє формулювання «5E compatible». Юридичну оцінку роби окремо, це не висновок аудиту.

---

## 2. Безпека (SEC)

### SEC-1 · Високий · Будь-який авторизований читає всю колекцію `users`
```
match /users/{userId} { allow read: if isSignedIn(); … }
```
`read` = `get` + `list` → перелічення всієї бази користувачів. Треба `get` лише для себе й пов'язаних, `list` заборонити, пошук по email — через `emailIndex`.

### SEC-2 · Критично · Захоплення `emailIndex` (найсерйозніше)
```
match /emailIndex/{emailLower} {
  allow create, update: if isSignedIn() && request.resource.data.uid == uid();
}
```
Немає перевірки, що **існуючий** документ належить тому ж користувачу → будь-хто перезапише `emailIndex/victim@example.com` своїм `uid`. Далі шаринг по email (`src/services/users.ts:24`) віддасть аркуш атакуючому. **З кількома редакторами це прямий шлях до чужого персонажа з правом запису.**

Фікс: `create: … && resource == null`, `update: … && resource.data.uid == uid()`, `list` заборонити.

### SEC-3 · Високий · Немає валідації форми документів при create/update
Ні для `characterSheets`, ні для `dmCampaigns`/`dmCampaignNotes` не перевіряються `keys()`, типи, довжини, розміри масивів. Будь-який запрошений редактор може писати документ довільної форми (див. також COL-8).

### SEC-4 · Середній · `connections`: `update: if isParticipant()` без immutability
Учасник може переписати `fromUid`/`toUid`. Зафіксувати незмінні поля так само, як зроблено для `characterSheets`.

### SEC-5 · Високий · `users` має `allow delete: if false`
Прямо конфліктує з PLY-1. Потрібен дозвіл власнику або каскад через Cloud Function.

### SEC-6 · Високий · Мертвий код звертається до колекцій без правил — ✅ Виправлено 2026-08-12
`src/shared/services/firestore/{firestore.ts,pdocs.ts,sharing.ts,loger.ts}` ніде не імпортуються (крім `pdocs → sharing`), але працюють із `docs`, `docShares`, `sharedDocs`, `demoItems` (яких немає в `firestore.rules`) і роблять `addDoc(collection(fs,'users'), …)`, тобто створюють сміттєві документи в `users` з випадковими id. **Видалити всі чотири файли** (~700 рядків).

**Перевірено 2026-08-12:** директорія `src/shared/services/firestore/` не існує — усі чотири файли видалено.

### SEC-7 · Середній · Уразливості залежностей — ЧАСТКОВО ЗАКРИТО 2026-07-31

Усі — у **build/dev-тулчейні**, не в рантаймі застосунку. `npm audit` на момент фіксу показав ширшу картину, ніж попередній запис (59, а не 23 — реєстр CVE в upstream пакетах зростав): **59** (2 critical / 41 high / 16 moderate) до фіксу → **21** (0 critical / 1 high / 20 moderate) одразу після. **Актуальний стан на 2026-07-31 (після відкату `brace-expansion`-override, див. таблицю й нотатку нижче): 22 (21 moderate, 1 high)** — цифра з таблиці вище вже застаріла для цього одного пакета.

**Закрито через `package.json` → `overrides`** (без мажорних апгрейдів, версії — мінімально достатні для патчу, не останні мажорні):

| Пакет | Було (резолвилось) | Стало | Чому саме ця версія |
| --- | --- | --- | --- |
| `tar` | 7.5.16 | `7.5.21` | перша непідпадаюча під `<=7.5.20` (DoS/crash CVE) |
| `shell-quote` | 1.8.3 | `1.9.0` | перша непідпадаюча під `<=1.8.4` (командна ін'єкція через newline в `.op`) |
| `undici` | 6.26.0 | `6.27.0` | перша непідпадаюча під `<=6.26.0`, лишились у мажорі 6.x (консюмери — `@expo/cli`, `firebase-tools`, `node-gyp` — вимагають `^6.x`) |
| `fast-uri` | 3.1.2 | `3.1.4` | перша непідпадаюча під `<=3.1.3` (host confusion), `ajv` вимагає `^3.0.1` |
| `protobufjs` | 7.6.2 | `7.6.5` | перша непідпадаюча під `<=7.6.4`, лишились у 7.x (консюмери вимагають `^7.x`) |
| `brace-expansion` | ~~`5.0.8` (єдина копія)~~ **ВІДКОЧЕНО 2026-07-31** — override видалено | кожна копія на своєму природному мажорі (`1.1.18`, `2.1.4`, `5.0.9` — залежно від консюмера) | Твердження нижче з попереднього фіксу було **хибним**: «API пакета не змінювався роками» — насправді `brace-expansion@5.x` більше не є прямим викликним `expand()`, а стара вендорена `minimatch` усередині `@react-native/codegen` (і в самому `react-native`) саме так його й викликає. Блоковий override зламав `gradlew :app:assembleRelease` на EAS (`TypeError: expand is not a function`, три Gradle-таски `generateCodegenSchemaFromJavaScript` — async-storage, google-signin, gesture-handler) — виявлено тільки при **реальному** EAS-білді, не при `npm ci`. Видалення override повертає кожен консюмер до його власного, вже й так пропатченого діапазону (`^1.1.7`→`1.1.18`, `^2.0.x`→`2.1.4` — обидва новіші за версію, в якій закрито `GHSA-mh99-v99m-4gvg`, окрім самого номера версії за формальним semver-порівнянням, через що `npm audit` далі рахує їх «вразливими» — це артефакт способу порівняння версій в аудиті, не реальна відсутність патчу для їхніх ліній) |
| `ws` | 6.2.3 (react-native, dev-middleware) / 7.5.10 (metro, firebase-tools, react-devtools-core) / 8.21.0 (`@expo/cli`, вже безпечний) | `6.2.4` для `react-native` і `@react-native/dev-middleware`; `7.5.11` за замовчуванням (metro/firebase-tools/react-devtools-core); `8.21.0` пришпилено для `@expo/cli` | три незалежні мажорні лінії в дереві — патчено кожну в межах її мажора через вкладені `overrides` (`react-native`, `@react-native/dev-middleware`, `@expo/cli`), щоб не зламати консюмерів, які тримаються за конкретний мажор |
| `js-yaml` | 3.14.2 (лише в `@istanbuljs/load-nyc-config`) / 4.3.0 (вже безпечний, лишили як є) | `3.15.0` — вкладений override тільки для `@istanbuljs/load-nyc-config` | `4.3.0` не займали: js-yaml 4.x має іншу дефолтну поведінку `load()` (нема `safeLoad`), тому мажорна заміна для консюмера, що очікує 3.x API, була б ризикованою |

**Лишилось до мажорного апгрейду** (усе тягне `expo@57` або `firebase-tools`-мажор, у build/dev-тулчейні, не в рантаймі застосунку):

`@expo/cli`, `@expo/config`, `@expo/config-plugins`, `@expo/metro-config`, `@expo/prebuild-config`, `expo`, `expo-asset`, `expo-constants`, `expo-dev-client`, `expo-dev-launcher`, `expo-splash-screen`, `postcss`, `xcode` — усі через `expo@57.0.9`; `@google-cloud/pubsub`, `@opentelemetry/core`, `gaxios`, `universal-analytics`, `uuid` — через мажор `firebase-tools`; `re2` — moderate, `fixAvailable: true`, але не входив у узгоджений список восьми, залишено на окрему задачу.

Єдина `high` уразливість, що лишилась, — `postcss` (`<=8.5.17`, фікс лише через `expo@57.0.9`).

**Expo SDK 57 до релізу не піднімати.** Формулювання для стейкхолдерів: це ризик для машини розробника й CI, а не RCE у публічному APK.

**Перевірено 2026-07-31 (перший прохід)**: лок-файл перегенеровано виключно через `npx -p node@20.19.4 -p npm@10 npm install --package-lock-only --include=dev --include=optional --include=peer`, потім `npx -p node@20.19.4 -p npm@10 npm ci --include=dev` — пройшло без помилок (1476 пакетів). `npm run validate` — 0 errors, 25 warnings (та сама лінія, що й до фіксу). `npm run test:unit` — 57 файлів / 279 тестів зелені. **Цього виявилось недостатньо**: жоден із цих кроків не запускає Gradle, тому несумісність `brace-expansion@5.x` зі старою вендореною `minimatch` не спливла — її показав лише реальний `eas build` (Gradle-таск `assembleRelease`). Висновок на майбутнє: для overrides, що чіпають пакети в ланцюжку `glob`/`minimatch`/codegen-тулінгу React Native, `npm ci` — недостатня перевірка, потрібен реальний Android-білд (`CLAUDE.md §4` це вже вимагав рядком «локальний Android-білд», його пропустили).

**Перевірено 2026-07-31 (після відкату override)**: `npx -p node@20.19.4 -p npm@10 npm install --package-lock-only --include=dev --include=optional --include=peer` → `npx -p node@20.19.4 -p npm@10 npm ci --include=dev` (1499 пакетів, без помилок). `npm run typecheck` — 0 помилок. `npm run lint` — 0 errors, 25 warnings (та сама лінія). `npm run lint:theme` — passed. `npm run test:unit` — 57 файлів / 279 тестів зелені. `npm audit` під `npm ci --include=dev` (без `--include=optional --include=peer`, що ближче до реального білду): **22** (21 moderate, 1 high — сам `brace-expansion`/`GHSA-mh99-v99m-4gvg`, формально «вразливий» за номером версії, реально вже на найновішому патчі своєї лінії). Обидва EAS-білди (`development`, `production`) перезапущено з цим лок-файлом.

**Перевірено 2026-08-12 (після додавання `expo-updates` для R4-1):** `npm audit` → **35** (20 moderate, 15 high) — зростання з 22, не через нову вразливість у нашому коді, а через ланцюжок `expo-updates → expo-manifests/expo-dev-client/expo-dev-launcher/expo-splash-screen → @expo/prebuild-config → @expo/config-plugins → xcode` (той самий build-toolchain кластер, що вже фігурував вище, підключений через ще одну залежність). Класифікація "білд/dev-тулчейн, не рантайм APK" з §SEC-7 і CLAUDE.md §8.4 продовжує діяти — жодна з нових high лежить не в коді, який виконується на пристрої користувача. Цифру в `CLAUDE.md` §2 і `docs/release-plan-google-play.md` оновлено на актуальну; закриття лишається можливим лише через `expo@57` (заборонено до релізу).

**Перевірено 2026-08-13:** `npm audit` → **38** (21 moderate, 17 high), зростання з 35. Новий пакет у списку — `image-size` (через `metro`, той самий build-тулчейн-кластер, DoS через нескінченний цикл у ICNS/JXL/HEIF-парсерах — використовується лише під час бандлінгу, не в застосунку на пристрої). Другий новий пакет — `nanoid` — розібраний окремо в **SEC-11**, бо він не вписується в чисту класифікацію «build-тулчейн».

### SEC-11 · Низький (практично) / Високий (формально) · `nanoid` — вперше runtime-залежність у списку `npm audit`

На відміну від решти записів SEC-7, `nanoid <3.3.17` (`GHSA-28wg-ghj8-5hjv`, `GHSA-2v37-7h3g-55p8`) підключений не лише через build-тулчейн (`postcss`), а й напряму через **`@react-navigation/native` → `@react-navigation/routers`** — цей пакет виконується в самому бандлі застосунку на пристрої користувача (`npm ls nanoid` показує три шляхи: `@react-navigation/*`, `@expo/metro-config → postcss`, dev-залежність `vite → postcss`).

**Чому це не практичний ризик:** обидва advisory — «нескінченний цикл при виклику генератора з негативним або нульовим `size`». `@react-navigation/routers` викликає `nanoid()` без аргументів (дефолтний розмір), і в кодовій базі проєкту немає жодного прямого викорисання `nanoid` з користувацьким/обчислюваним `size` (перевірено `grep -rn "nanoid" src/` — 0 збігів, пакет не імпортується напряму, лише транзитивно через react-navigation). Шляху, яким атакуючий міг би передати негативний/нульовий розмір у виклик, немає.

**Чому це все ж варто відзначити окремо від SEC-7:** класифікація «усе в build/dev-тулчейні, не в рантайм APK», яка виправдовує весь інший `npm audit`-регрес (CLAUDE.md §8.4, §2), для цього одного пакета неточна — код справді виконується на пристрої. Правильне виправдання тут інше: код не викликає вразливий шлях, а не «пакет не потрапляє в APK».

**Дія (не зроблена в межах цього аудиту):** `fixAvailable: true`, без мажорного апгрейду (`nanoid@3.3.17`+). Можна закрити через `package.json` → `overrides`, за тим самим протоколом, що й решта SEC-7 (мінімально достатня версія, потім `npm ci` + `validate` + `test:unit` + **реальний Android-білд** — минулий досвід із `brace-expansion` показав, що саме Gradle-білд, не `npm ci`, ловить несумісності в цьому шарі залежностей). Не зроблено зараз, бо це окрема зміна з окремою валідацією, а не документаційний прохід.

### SEC-8 · Середній · Секрети й ключі в репозиторії
`.env` і `google-services.json` закомічені (в `.gitignore` рядки закомментовані «для білду»). Firebase Android API key не секретний за дизайном, але обмеж його в Google Cloud Console по package + SHA-1 (включно з Play App Signing), і пам'ятай: правила Firestore — єдиний реальний захист даних.

### SEC-9 · Низький · 27 `console.*` у прод-коді — ✅ Перевірено безпечно 2026-08-12 (кількість 27→39 — гігієна, не витік)
`characterCloudRepository.ts`, `services/connections.ts`, `CharacterMenu.tsx`, `Settings.tsx`, `fileSerice.ts` та ін.

**Перевірено 2026-08-12:** `grep -rn "console\.\(log\|warn\|error\|info\|debug\)" src/` (без тестів) → **39 входжень**, було 27. Кількість зросла разом із роботою над Crashlytics/Analytics/Campaign Management, але **усі 39 переглянуто вручну**: кожне обгорнуте `if (__DEV__)` / `if (isDev)` (де `isDev = typeof __DEV__ !== 'undefined' && __DEV__`), окрім двох у `startupTrace.ts:73,75` — ті лежать за раннім `return` під `isStartupTraceEnabled()` (вмикається лише `EXPO_PUBLIC_STARTUP_TRACE=1`). Жоден виклик не виконається в релізному білді без явного opt-in. Попереднє формулювання «друкує вміст документів у logcat релізного білда» для поточного коду не підтвердилось. Задача лишається відкритою як гігієна коду (зменшити кількість debug-логів), не як безпековий ризик.

### SEC-10 · Критично · `characterSheets` — власне правило `get`/`list` блокувало створення НОВОГО персонажа — ✅ Виправлено 2026-08-13

**Симптом (знайдено власником продукту на реальному пристрої):** створення персонажа з режимом «Локально + Хмара» завжди завершувалось `[firestore/permission-denied] The caller does not have permission to execute the specified operation.` — і для щойно створеного персонажа, і для вже існуючого. Не залежало від релогіну через Google чи повного перевстановлення застосунку.

**Корінна причина:** `upsertCharacterSheetFromLocal` (`characterCloudRepository.ts:429-452`) перед створенням документа робить `const snap = await ref.get();`, щоб вирішити create-чи-update. Але правило `allow get, list: if isEditor();` читає `resource.data.ownerUid`/`owners` — а для документа, який **ще не існує**, `resource` дорівнює `null`. Читання `resource.data.*` на `null`-ресурсі за дизайном Firestore завжди обчислюється як `false`, тож сам `.get()`-запит на перевірку існування падав з `permission-denied` ще до того, як код доходив до `ref.set()` — документ персонажа ніколи не потрапляв у Firestore. Помилки `Listen`, які видно в `adb logcat` (`Listen for QueryWrapper(...characterSheets/{id}...) failed: PERMISSION_DENIED`), — це наслідок (підписка на документ, якого справді не існує), а не причина.

Розслідування пройшло через кілька хибних гіпотез перед цим (рейс-кондишн у транзакційному фолбеку синку — реальний, але окремий баг, див. нижче; App Check — не підключений у проєкті; зсув годинника — спростовано, бо `users/{uid}`-запис з тим самим `request.auth` у ту саму секунду проходив успішно). Остаточно підтверджено `adb logcat` (документа немає у Firestore Data browser) і відтворено на локальному Firestore-емуляторі (`firebase emulators:exec` + `@firebase/rules-unit-testing`).

**Фікс:** `firestore.rules`, `characterSheets` → `allow get, list: if resource == null || isEditor();`. Дозволяє клієнту безпечно перевірити «документ ще не існує» (там немає даних, тож нічого не витікає) без зміни захисту вже існуючих документів.

**Перевірено на емуляторі** (6 сценаріїв, `@firebase/rules-unit-testing`): (1) `get()` на неіснуючому id — тепер `exists:false` замість `permission-denied`; (2) створення власником — працює; (3) власник читає свій документ — працює; (4) інший користувач перевіряє чужий-майбутній (неіснуючий) id — безпечно `exists:false`; (5) інший (не власник/редактор) читає **існуючий** чужий документ — і досі коректно заборонено; (6) `list`-запит `owners array-contains uid` повертає лише свої документи. Підтверджено власником продукту на реальному пристрої після деплою правил: створення персонажа «Локально + Хмара» більше не падає.

**Супутній, окремий фікс того ж дня:** транзакційний фолбек-шлях у `upsertCharacterSheetFromLocal` (залишок COL-1, див. розділ 3) мав власну гонку — зовнішній нетранзакційний `ref.get()` міг показати «документ існує» зі stale-кешу до того, як перший `.set()` фактично підтвердився на сервері, і фолбек тоді писав контент-only payload (без `owners`/`editors`) для документа, який транзакційний `tx.get()` бачив як ще не існуючий → та сама `permission-denied`. Виправлено: транзакція тепер сама перевіряє існування своїм `tx.get()` і вибирає повний «власницький» payload, якщо документа справді ще нема. Регресійний тест: `characterCloudRepository.test.ts` — «sends the full ownership payload in the transactional fallback when the doc does not exist yet, even if the plain get() said it did».

---

## 3. Спільне редагування (COL) — новий розділ v2

Повний розбір, цільова модель і тест-план — у `docs/collaborative-editing.md`. Тут короткий реєстр:

| ID | Severity | Проблема | Файл |
| --- | --- | --- | --- |
| **COL-1** | Критично | `upsertCharacterSheetFromLocal` = `get()` → зібрати **весь** документ → `set(merge:true)`. Read-modify-write без транзакції → паралельний редактор тихо перезаписує чужі зміни. `merge:true` не рятує, бо payload містить усі поля | `characterCloudRepository.ts:409-447` |
| **COL-2** | Критично | `changeHistory` пишеться цілим масивом зі stale-read → стирає записи іншого редактора. А на журналі тримається `remotePathsSinceLastSync`, тож чужі зміни перестають виявлятися взагалі. Плюс ліміт 50 записів | `characterCloudRepository.ts:398-429` |
| **COL-3** | Критично | `owners`/`editors` беруться зі stale-read і пишуться назад: owner відкатує щойно доданого редактора; editor отримує `PERMISSION_DENIED` (правило вимагає незмінності `editors`) — і помилка глушиться | `characterCloudRepository.ts:433-440` + `firestore.rules` |
| **COL-4** | Високий | Секція `combat` = hp + ac + initiative + speed + hitDice + deathSaves + weapons + conditions. `collectConflictPaths` еквалює конфлікт на рівні секції → DM крутить HP, гравець ставить condition = конфлікт-модалка або втрата секції. Найчастіший сценарій живої сесії | `conflictPolicy.ts`, `characterSyncCoordinator.ts` |
| **COL-5** | Високий · ✅ Виправлено 2026-08-12 | `changeHistory[].atMs` і `lastSyncAt` — клієнтський `Date.now()`; порівнювались між різними девайсами. Зсув годинника → або пропущені чужі зміни, або нескінченні повторні merge | `characterCloudRepository.ts:429-488`, `characterSyncCoordinator.ts:588-621`, `useCharacterActions.tsx:644-674` |
| **COL-6** | Середній | Немає presence: не видно, хто ще в аркуші. Конфлікти виглядають випадковими. Дешевий фікс — «DM оновив Combat 12 с тому» з наявного журналу | `src/screens/Character/*` |
| **COL-7** | Середній | Помилки шляху синку глушаться (`bulkUpsertFromLocal` порожній catch, `saveCharacters` catch, `.catch(()=>{})` на маркерах) → відмова запису невидима користувачу | `characterCloudRepository.ts:449-455`, `characterLocalRepository.ts:48` |
| **COL-8** | Середній | Правила не валідують форму документа, який пише редактор (див. SEC-3) | `firestore.rules` |
| **COL-9** | Середній | Ризик ліміту 1 MiB: вміст + homebrew + notes + 50 записів журналу в одному документі. При перевищенні запис падає — і, через COL-7, тихо | `characterCloudRepository.ts` |
| **COL-10** | Високий | Видалення акаунта при спільних аркушах: наївний каскад знищить чужі дані. Потрібна логіка owner/editor + передача ownership + Cloud Function | пов'язано з PLY-1 |

**Головний висновок розділу:** COL-1 + COL-2 + COL-3 — це один і той самий кореневий дефект (запис усього документа зі застарілого читання), і він лікується однією зміною: content write пише **лише змінені шляхи** й не торкається полів доступу, журнал додається атомарно. Це найцінніша технічна робота в усьому релізі, бо вона стосується даних користувачів, а не косметики.

**COL-1/2/3 — ✅ Значною мірою виправлено 2026-08-12.** `characterCloudRepository.ts:429-488` тепер: новий документ — простий `set()`; існуючий документ із конкретним змінюваним шляхом — точкові `update()`/`deleteField()` по полях, `changeHistory` — через `arrayUnion` (не перезапис масиву). Лишається один фолбек-шлях (для "невідомого"/tab-default шляху), який досі робить читання-й-`set(merge:true)`, але вже **всередині `runTransaction`**, тобто вихідна небезпечна гонитва (запис без транзакції) закрита; звузити цей останній фолбек до точкових шляхів — залишок COL-1 для наступної ітерації.

**Додатково зміцнено 2026-08-13 (SEC-10 розслідування):** сам фолбек мав власну гонку — зовнішній `ref.get()` перед `runTransaction` міг звітувати «документ існує» зі stale-кешу до фактичного підтвердження першого `.set()` на сервері; транзакція тоді писала контент-only payload (без `owners`/`editors`) для документа, якого насправді ще нема → `permission-denied`. Транзакція тепер сама перевіряє існування (`tx.get()`) і обирає повний payload з `dtoToSheet()`, якщо документа справді нема. Не сама причина того конкретного бага (справжня причина — SEC-10 вище), але реальна й окремо виправлена дірка того самого фолбек-шляху.

**COL-5 — ✅ Виправлено.** `serverTimestamp()` не можна класти в елемент `arrayUnion`, тому замість підколекції (окрема задача, `docs/collaborative-editing.md` COL-5) обрано мінімальний варіант: серверний курсор `lastChangeAt` на документі `characterSheets` (`characterCloudRepository.ts:448,469,482`), а відбір нових шляхів робиться diff-ом за стабільним `entry.id` (`computeRemoteHistorySync`, `characterSyncCoordinator.ts:591-609`) — повністю незалежно від будь-якого клієнтського годинника. `syncState.lastSyncAt` тепер зберігає серверний час (`serverSyncAtMs` з `lastChangeAt`) останнього застосованого віддаленого знімка, а не `Date.now()` того, хто читає. `changeHistory[].atMs` лишається клієнтським — свідомо, він більше ніде не бере участі в порівнянні для визначення нових шляхів, тільки у відображенні "N хв тому" в UI (`Character.tsx:158`, `DMSharedUpdates.tsx:365`), де неточність під зсувом годинника — косметика, не втрата даних. Тести на зсув годинника ±10 хв — `characterSyncCoordinator.test.ts:433-518`.

---

## 4. Продуктивність (PERF)

### PERF-1 · Середній (перекваліфіковано) · Холодний старт вирішено, залишкова вартість — на першому відкритті екрана, не на старті — ⚠️ Пом'якшено 2026-08-13
`AppNavigator.tsx` → `TabNavigator.tsx` статично імпортують усі екрани → ті тягнуть `@/domain/srd/*` → `@/data/srd`.

**Перевірено 2026-08-13: план нижче вже реалізований, опис вище був застарілим.**
- `src/domain/srd/srdRepository.ts` — усі 11 колекцій парсяться лінивo, "at most once, on first access" (коментар `PERF-1`), Zod перенесено в build-time (`scripts/validate-srd.mjs`, `npm run validate:srd`), рантайм — типізований каст.
- `src/domain/srd/localization.ts` — `uk/monsters.json`/`uk/spells.json` (843+628 КБ) і 3 `Map` будуються лінивo через `ensureLocalizationLoaded()`, теж "at most once".

Замір у vitest (x86, з transform-оверхедом, **не показник для телефона**): json ~1279 мс, zod ~176 мс, uk-локалізація ~1017 мс, разом ~2472 мс. Ця вартість більше не блокує перший кадр — але лінива підвантажка все ще виконувалась синхронно всередині `useMemo` того самого рендеру, який знімає скелетон на `Bestiary`/`Spellbook`/Character (перше відкриття екрана за сесію = «зависання без скелетона», знахідка тестувальника 2026-08-13). Пом'якшено: `warmSrdLocalizationCache()`/`warmCharacterSrdCache()` викликаються в `useEffect` через `InteractionManager.runAfterInteractions` на вході в ці три екрани, щоб прогрів відбувався під час скелетона. **Не перевірено на реальному Android-девайсі.**

### PERF-2 · Високий · R8/ProGuard і shrinkResources вимкнені — ✅ Виправлено 2026-08-14
`app/build.gradle` читає `android.enableProguardInReleaseBuilds` і `android.enableShrinkResourcesInReleaseBuilds`, яких немає в `gradle.properties` → обидва `false`. Увімкнути + правила для RN Firebase/Reanimated + **повний smoke-тест релізного білда**.

**Перевірено 2026-08-14:** обидва прапорці ввімкнено в `android/gradle.properties` (окремими рядками, з `// manual:`-коментарем). У `android/app/proguard-rules.pro` додано keep-правила для бібліотек без власних `consumerProguardFiles` — перевірено пряме читання `node_modules/**/android/build.gradle` кожної залежності, а не здогадки: `react-native-screens` (Fragment-рефлексія), `react-native-gesture-handler`, `react-native-keyboard-controller` (та сама бібліотека з REL-9 нижче), `react-native-pager-view`, `io.invertase.firebase` (РН-міст `@react-native-firebase`, не сам Google Firebase SDK). Свідомо **не** додано правил для `react-native-reanimated`/`react-native-worklets`/`react-native-svg`/`expo-modules-core` — усі чотири вже автоматично зливають власні keep-правила через `consumerProguardFiles`, підтверджено в змердженому `configuration.txt` після реальної збірки.

Обидва прапорці (minify окремо, потім shrinkResources) реально зібрані локально: `ANDROID_HOME=/usr/lib/android-sdk ./gradlew :app:bundleRelease` — **BUILD SUCCESSFUL** двічі. Підтверджено по артефактах, не лише exit code: `mapping.txt` (54 МБ) згенеровано й підхоплено `uploadCrashlyticsMappingFileRelease`; фінальний `configuration.txt` містить усі ручні keep-правила плюс автозлиті від бібліотек вище. Знайдено (не створено цією зміною) 30 нефатальних R8-попереджень «Invalid stack map table» виключно в `play-services-auth-21.5.0-runtime.jar` (Google Sign-In) — відомий, зазвичай нешкідливий клас попереджень при верифікації вже-обфускованого байткоду Google Play Services; білд успішний обидва рази. Розмір AAB після shrinkResources: 79 833 505 → 79 403 987 байт (ресурс-шрінкер у консервативному режимі через `Resources#getIdentifier` десь у коді/залежностях).

**Не перевірено:** встановлення цього AAB на реальний пристрій і повний ручний smoke-тест (у цьому середовищі немає підключеного пристрою/емулятора) — чекліст нижче ще належить пройти власнику продукту.

### PERF-3 · Середній · Невикористані залежності — ✅ Виправлено 2026-08-12
0 імпортів у `src`: `react-native-fs`, `react-native-vector-icons`, `react-navigation@^5.0.0`, `expo-media-library`, `expo-intent-launcher`, `react-native-uuid`. `react-native-fs` ще й без підтримки New Architecture. Іконки беруться з `@expo/vector-icons`.
**Не знімати**: `react-native-pager-view`, `react-native-tab-view` (peer для `@react-navigation/material-top-tabs`).

**Перевірено 2026-08-12:** усі 6 пакетів відсутні в `package.json`.

### PERF-4 · Низький · `App.tsx:7` — `import 'expo-dev-client'` безумовний — ✅ Виправлено 2026-08-12
Обгорнути в `if (__DEV__)`.

**Перевірено 2026-08-12:** `App.tsx:9-14` — імпорт тепер за `if (__DEV__) { require('expo-dev-client'); }`, позначено коментарем `PERF-4`.

### PERF-5 · Середній · Біла пауза на старті — ⚠️ Частково пом'якшено 2026-08-12
`App.tsx:31-33` — `return null` до готовності i18n, без splash-контролю, хоча `expo-splash-screen` встановлений.

**Перевірено 2026-08-12:** додано `SplashScreen.preventAutoHideAsync()` і приховування лише після готовності i18n (`App.tsx:26-28,81-86`, тег `PERF-5`) — нативний splash тепер тримається замість білого екрана. Сам `if (!isI18nReady) return null` (`App.tsx:88-90`) усе ще є, але вже не показує порожній білий кадр користувачу, бо його прикриває splash.

### PERF-6 · Середній · `FlatList` без тюнінгу на найдовших списках — ✅ Виправлено (документ не був оновлений)
`Bestiary.tsx:297`, `Spellbook.tsx:710` — лише `keyExtractor`. Правильний приклад є в `CharacterModals.tsx:481-483`.

**Перевірено 2026-08-13:** обидва файли вже мають `initialNumToRender={10}`, `maxToRenderPerBatch={10}`, `windowSize={7}`, `removeClippedSubviews` (`Bestiary.tsx:417-420`, `Spellbook.tsx:808-811`). Опис вище був застарілим.

### PERF-7 · Низький · Іконки 497 КБ ×3
`assets/icon.png`, `adaptive-icon.png`, `splash-icon.png` — три ідентичні файли. Стиснути при заміні на новий логотип.

### PERF-8 · Середній · Кількість підписок Firestore (новий у v2)
`CharacterCard` робить `subscribeCharacterSheet` **на кожну картку** на Home, плюс `subscribeMySheets` і `subscribeSharedWithMe` — дані частково дублюються. З 10 персонажами це 12 слухачів. Зі спільним редагуванням і живими сесіями читання множаться → батарея й квота. Порахувати очікувані читання на активного користувача за сесію.

---

## 5. Надійність і код-здоров'я (REL)

| ID | Проблема | Де |
| --- | --- | --- |
| **REL-1** | ✅ Виправлено 2026-08-12. Немає root `ErrorBoundary` → біла сторінка без діагностики. Критично без Crashlytics. **Перевірено:** `App.tsx:20,93,108` імпортує й обгортає дерево `src/shared/components/ErrorBoundary/ErrorBoundary.tsx` (+ `ErrorBoundary.test.tsx`); Crashlytics також підключено (`App.tsx:7,38-52`) | `App.tsx` |
| **REL-2** | ✅ Виправлено. `saveCharacters` глушить помилки → тиха втрата даних персонажа. **Перевірено 2026-08-12:** функція більше не має `try/catch` — кидає помилку далі, коментар у коді посилається на `COL-7`/`REL-2`, виклик у `characterStoreEffects.saveCharacters` має її обробити (сам UI-виклик окремо не простежено) | `characterLocalRepository.ts:35-41` |
| **REL-3** | `useCharacterActions.tsx` — **3494 рядки**, тримає більшість із 25 lint-ворнінгів (`exhaustive-deps` на `characterData.hp`, `t`, `currentSync`). Ворнінги про мутабельні залежності — реальні кандидати на баг «HP не оновився». До релізу не розпилювати, але HP-флоу перевірити руками | `src/screens/Character/hooks/useCharacterActions.tsx` |
| **REL-4** | ℹ️ **Закрито рішенням 2026-08-12 (не буде зроблено).** Немає CI: `.github/` містить лише `instructions/`. Власник продукту: e2e-автоматизація не планується, регресію тестують вручну — це свідоме рішення, не прогалина | `.github/` |
| **REL-5** | Немає тестів правил Firestore в емуляторі — найризиковіша частина системи | `firestore.rules` |
| **REL-6** | ⚠️ **Частково виправлено 2026-08-12.** `app.json`: `userInterfaceStyle: "light"` при наявній темній темі; білий `splash.backgroundColor`. **Уточнення:** твердження про білий `splash.backgroundColor` було застарілим — і в `app.json`, і в нативних `android/.../colors.xml`/`styles.xml` фон сплеша вже брендовий червоний `#EE1B24` (позначено `// manual: brand refresh`). Реальний баг був не в самому сплеші, а в тому, що `AppTheme` (тема, яка вмикається одразу після приховування сплеша) не мала `android:windowBackground` і тому на мить показувала стандартний білий фон AppCompat — саме це й «злякало тестувальника». **Виправлено:** `AppTheme` тепер має `android:windowBackground = @color/splashscreen_background` (`android/app/src/main/res/values/styles.xml`). **Не виправлено:** сам вибір `userInterfaceStyle: "light"` (примусовий світлий режим незалежно від системної теми) — продуктове рішення, не технічний баг | `app.json`, `android/app/src/main/res/values/styles.xml` |
| **REL-7** | `.expo/devices.json` у архіві; `.codex/` — порожній каталог | — |
| **REL-8** | ✅ Виправлено 2026-08-13. Crashlytics Gradle plugin (`firebase-crashlytics-gradle:3.0.6`) підключений, але без `firebaseCrashlytics { nativeSymbolUploadEnabled true }` у `buildTypes.release` не завантажував власні нативні символи (`libappmodules.so`/`libreactnative.so`) — усі відкриті native-краші релізу `1.0.0 (7)` показані з `(Missing BuildId ...)`, без резолвленого стеку. Виявлено при перегляді Firebase Crashlytics власником продукту (3 open issues: SIGABRT `libc.so` regressed, 2× SIGSEGV). **Наслідок:** символи в тих трьох issue не відновлюються ретроактивно — потрібен новий build + новий crash того ж типу для перевірки | `android/app/build.gradle` |
| **REL-9** | ✅ Виправлено 2026-08-14. Два нові Crashlytics issue, версія `1.0.2 (8)`: (1) `null pointer dereference: SIGSEGV 0x10`, стек через `libworklets.so`→`libhermes.so`; (2) `SIGSEGV`, стек через `libreactnative.so`→`libfbjni.so`. Обидва без резолвлених символів (Crashlytics показує «Missing BuildId» для `libhermes.so`/`libworklets.so`/`libreactnative.so`/`libfbjni.so`), тож точну лінію коду встановлено архітектурним аналізом, не по стектрейсу. **Корінна причина:** RN `<Modal>` на Android розмонтовує вміст синхронно (без exit-анімації, на відміну від iOS) в момент, коли `visible` стає `false`; якщо клавіатура в цей момент ще закривається, `KeyboardAwareScrollView` (`react-native-keyboard-controller`) усередині спільного `src/shared/components/Modal/Modal.tsx` намагається виконати worklet проти вже знищеної Fabric-view. Додатково: окрема модалка вибору заклять у створенні персонажа (крок 7 візарда) рендерила ще один вкладений `KeyboardAwareScrollView` **усередині** вже наявного зовнішнього `KeyboardAwareScrollView` майстра — сам факт монтування (навіть невидимої модалки) крашив екран одразу при переході на крок 7. **Фікс:** `Modal.tsx` тепер тримає вміст змонтованим, доки `KeyboardController.dismiss()` не підтвердить закриття клавіатури, перш ніж зняти `RNModal.visible` (не зачіпає жоден виклик — 11 місць використання без змін). Модалка вибору заклять переведена на звичайний RN `Modal`+`ScrollView` без `react-native-keyboard-controller` — їй узагалі не потрібне уникнення клавіатури (немає текстових полів) | `src/shared/components/Modal/Modal.tsx`, `src/screens/CreateCharacter/CreateCharacter.tsx` |
| **REL-10** | ✅ Виправлено 2026-08-14. Реальний `logcat` з підключеного пристрою (Motorola Edge 70 Fusion, Android 16) показав **4 ідентичні native-tombstone** сьогодні: `Fatal signal 6 (SIGABRT)`, `Abort message: 'Pointer tag ... was truncated'` (Android-перевірка тегованих вказівників ловить double-free/use-after-free), стек — `libc: free()` ← `facebook::react::RNCAndroidDialogPickerProps::~RNCAndroidDialogPickerProps()` ← каскадне знищення Fabric shadow-tree. Крашило одразу після завершення створення персонажа (екран із ~10 `<Picker>` розмонтовується одним махом). **Корінна причина:** відомий баг `@react-native-picker/picker` у зв'язці з New Architecture на RN 0.81, виправлений у релізі `2.11.3` («android crash on React Native 0.81 & new arch», PR #648) — у проєкті був закомічений `2.11.1`, до фіксу. **Фікс:** `@react-native-picker/picker` 2.11.1 → `2.11.4` (остання патч-версія лінії, включно з фіксом), плюс попутно `expo` `~54.0.35` → `~54.0.36` (patch-mismatch з `expo-doctor`). `expo.install.exclude` в `package.json` доповнено (`react-native-keyboard-controller`, `@react-native-picker/picker`) — обидва навмисно новіші за базову лінію Expo SDK 54, `expo-doctor` тепер 17/17. **Перевірено на реальному пристрої** (не лише статично): перебудова + встановлення через `npx expo run:android`, власник продукту підтвердив — краш більше не відтворюється, і суб'єктивні «лаги» під час гри з персонажем (кидки, HP) також зникли | `package.json`, `package-lock.json` |

---

## 6. EAS Update і Analytics — поточний стан

**EAS Update — ✅ Реалізовано 2026-08-12 (частково):** `expo-updates` тепер у `package.json` (`~29.0.19`); `app.json` містить `updates.url`, `runtimeVersion: "1.0.0"`, `fallbackToCacheTimeout`; `AndroidManifest.xml` має `ENABLED=true` + updates-метадані. `eas.json` навмисно має `channel: "production"` лише в профілі `production` (обґрунтування — `docs/release-plan-google-play.md` §R4-1: `preview` лишається інструментом ручного QA, не окремою OTA-веткою) — це не пробіл. **Лишається відкритим (R4-2):** живий тест OTA (`eas update --branch <production-branch>` → встановлений білд підхоплює апдейт) ще не проведено на реальному пристрої. Див. `docs/release-blockers.md`.

**Analytics — ✅ Реалізовано 2026-08-12:** готовий фасад `src/shared/services/telemetry/productTelemetry.ts` — типізований union `ProductEventName`, черга записів, локальний буфер 250 подій. Точки виклику: `Home.tsx`, `useCharacterActions.tsx`. Firebase Analytics підключено всередині модуля (`@react-native-firebase/analytics`), Crashlytics — `App.tsx:7,38-52` з `CrashlyticsUserBinding`, прив'язаним до перемикача згоди (`analyticsConsentEnabled`). Перемикач згоди в Settings реалізовано. Події конфліктів/шарингу/permission-denied для спільного редагування — **не перевірено окремо в цій сесії**, звірити з переліком у CLAUDE.md §8.1 як наступний крок.

---

## 7. Зведений список за пріоритетом

**P0 — блокує допуск або втрачає дані користувачів**
1. PLY-9 створити Play Console, з'ясувати тип акаунта, залити перший AAB у closed testing
2. COL-1 + COL-2 + COL-3 — один кореневий фікс запису (точкові шляхи, атомарний журнал, доступ окремо)
3. SEC-2 захоплення `emailIndex`, далі SEC-1, SEC-3, SEC-4
4. ~~PLY-1 + COL-10 + SEC-5 видалення акаунта з коректним каскадом~~ — ✅ Закрито 2026-08-13, підтверджено на пристрої
5. PLY-2 Privacy Policy (з розкриттям передачі даних між користувачами)
6. PLY-3 чистка permissions + PERF-3
7. PLY-4 підпис релізу + новий SHA-1 у Firebase/OAuth
8. PLY-6 версії 1.0.0 у двох файлах
9. SEC-6 видалити мертвий Firestore-код

**P1 — сильно впливає на якість релізу**
10. COL-4 дрібніші секції (мінімум розбити `combat`)
11. COL-5 серверний час у журналі й `lastSyncAt`
12. COL-7 видимі помилки синку + REL-2
13. PERF-1 холодний старт
14. Analytics + Crashlytics + перемикач згоди + REL-1 ErrorBoundary
15. SEC-7 `overrides` для 8 транзитивних CVE
16. PLY-7 edge-to-edge на Android 15/16
17. EAS Update із живим тестом
18. ~~PERF-2 R8/shrinkResources + smoke-тест~~ — ✅ прапорці й правила зроблено 2026-08-14, ручний smoke-тест на пристрої ще належить пройти
19. REL-5 тести правил в емуляторі, COL-8 валідація форми документа

**P2 — до релізу за наявності часу**
20. COL-6 «хто щойно правив» у шапці
21. Новий логотип/сплеш/іконки + PERF-7
22. PERF-4, PERF-5, PERF-6, PERF-8, SEC-9
23. REL-4 CI, REL-6, REL-7
24. Синхронізація `.agents/*`

**Після 1.0**
- Expo SDK 54 → 57 (закриває решту 15 CVE)
- COL-4 повністю: семантичний merge лічильників (`increment`), `arrayUnion` для conditions
- COL-6 справжній presence, COL-9 журнал у підколекцію
- REL-3 розпил `useCharacterActions.tsx`
- GM Desktop Workspace
